<h2>プロフィール編集</h2>
<%= form_with model: @user, method: :patch do |f| %>
  <label for="inputName">Name</label>
  <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"名前" %>
  <label for="inputImage">ProfileImage</label>
  <!--以下においてコロンは二種類の使われ方をしている、:profile_imageはシンボルとして-->
  <!--accept: "image/*"はf.file_fieldメソッドの引数acceptの値を指定している。ここで、acceptはキーで"image/*"が値のハッシュ形式の引数である。-->
  <!--Railsのフレームワークではオプションの引数にハッシュ形式を用いることが多い。-->
  <%= f.file_field :profile_image, accept: "image/*", placeholder: "プロフィール画像" %>
  <%= f.submit "変更を保存" %>
<% end %>